Simple template to run .NET Mono with Vagrant
=============================================

Naturally Vagrantfile should be on a project repository, but this is stored on its own repository to act as a template for other projects.
